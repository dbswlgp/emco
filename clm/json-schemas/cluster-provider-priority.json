{
    "$schema": "http://json-schema.org/schema#",
    "type": "object",
    "required": ["priority"],
    "properties": {
      "priority": {
        "type": "integer",
        "minimum": 1,
        "maximum": 10,
        "default": 10,
        "description": "Priority of cluster provider to be used"
      }
    }
  }

